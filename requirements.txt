# IMS Agent — Python dependencies
# Install with: pip install -r requirements.txt

# Anthropic SDK
anthropic>=0.40.0

# Data processing
pandas>=2.2.0
numpy>=1.26.0

# Environment variable loading
python-dotenv>=1.0.0

# Testing
pytest>=8.0.0
pytest-cov>=5.0.0

# Type checking (dev)
mypy>=1.10.0

# =============================================================================
# Phase 2 — Voice Interview Layer
# =============================================================================

# TTS — ElevenLabs (current) and Azure (Phase 5 migration target)
elevenlabs>=1.9.0

# STT — Whisper (local, no API key needed)
# NOTE: also requires system ffmpeg: https://ffmpeg.org/download.html
# openai-whisper>=20231117   # Uncomment when real audio STT is needed
#   pulls in torch automatically (~2 GB); not needed for simulation mode

# Audio playback (optional — only needed for playing TTS audio live during calls)
# sounddevice>=0.4.6
# soundfile>=0.12.1
